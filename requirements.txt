# Pinned to a recent known-good MINOR version with '>=' rather than exact '==' pins.
# Rationale: exact pins risk a broken resolution before this environment has been verified
# end-to-end (LangChain went through a 1.0 reorg in 2025 that split packages and moved
# import paths - see rag/ package docstrings for which module lives where). If `pip install`
# fails to resolve, relax these further (drop minor-version floors, or pin only the packages
# that actually conflict) rather than fighting the resolver.

# --- LangChain core + split packages (post-1.0 reorg) ---
langchain>=1.0.0
langchain-core>=1.0.0
langchain-classic>=1.0.0
langchain-community>=0.4.0
langchain-aws>=1.0.0
langchain-chroma>=0.2.0

# --- Vector store + retrieval ---
chromadb>=0.5.0
rank_bm25>=0.2.2

# --- AWS (Bedrock provider - kept working, see rag/llm/bedrock_llm.py) ---
boto3>=1.35.0
botocore>=1.35.0

# --- Azure (default provider, see rag/llm/azure_llm.py) ---
azure-ai-inference>=1.0.0b4
azure-core>=1.30.0

# --- CLI / API / UI ---
typer>=0.15.0
fastapi>=0.115.0
uvicorn>=0.32.0
streamlit>=1.38.0

# --- Config / validation ---
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# --- Document parsing ---
python-frontmatter>=1.1.0
PyYAML>=6.0.2

# --- Reporting ---
tabulate>=0.9.0

# --- Testing ---
pytest>=8.3.0
pytest-mock>=3.14.0
